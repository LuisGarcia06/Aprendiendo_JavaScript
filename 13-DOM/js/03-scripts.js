// Busca en todo el documento el elemento que tenga id="formulario"
// A diferencia de getElementsByClassName, devuelve UN SOLO elemento (los IDs son únicos)
const formulario = document.getElementById('formulario');
console.log(formulario); // <form id="formulario">...</form>  →  si existe
                         // null                              →  si NO existe



                         